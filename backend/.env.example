# ===== Bidly – Symfony .env.example =====
# Bu dosyayı kopyalayıp backend/.env.local olarak kullanın.
# Production secret değerleri buraya yazma!

### Symfony
APP_ENV=dev
APP_DEBUG=1
APP_SECRET=change_me_please
# Yeni secret üretmek için:
# php -r "echo bin2hex(random_bytes(32)).PHP_EOL;"

### Database (PostgreSQL – Docker container "db")
DATABASE_URL="postgresql://symfony:symfony@db:5432/bidly?serverVersion=15&charset=utf8"
# Eğer host makineden (DBeaver vs.) bağlanacaksan:
# host=localhost port=5433 user=symfony password=symfony db=bidly

### Redis (cache / session)
REDIS_URL="redis://redis:6379"

### RabbitMQ (Symfony Messenger için)
MESSENGER_TRANSPORT_DSN="amqp://guest:guest@rabbitmq:5672/%2f"

### Elasticsearch
ELASTICSEARCH_URL="http://elasticsearch:9200"

### Mail (development: mail atma, dummy)
MAILER_DSN="null://null"

### Frontend (CORS için köken adresi)
FRONTEND_URL="http://localhost:5173"

### Backend base (Nginx üzerinden)
APP_BASE_URL="http://localhost:8080"

# İleride WebSocket / Mercure eklersen:
# MERCURE_URL=http://mercure/.well-known/mercure
# MERCURE_PUBLIC_URL=http://localhost:3000/.well-known/mercure
# MERCURE_JWT_SECRET=change_me
